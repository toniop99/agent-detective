---
title: Agent Detective
description: AI-powered code analysis, plugins, and the PR pipeline. Pick a path below or use the sidebar.
template: splash
hero:
  title: Agent Detective
  tagline: AI-powered code analysis with plugins for Jira, Linear, and more. Self-hosted, open source, and extensible.
  actions:
    - text: Get started
      link: /docs/operator/installation/
      icon: right-arrow
      variant: primary
    - text: Build a plugin
      link: /docs/plugins/extending-with-plugins/
      variant: minimal
---

import { Card, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';

export const docsBase = import.meta.env.BASE_URL.endsWith('/')
	? import.meta.env.BASE_URL
	: `${import.meta.env.BASE_URL}/`;

## Choose a path

<CardGrid stagger>
	<LinkCard
		title="Run the server"
		href={docsBase + 'operator/installation/'}
		description="Install with Docker, Compose, or bare metal; configure, upgrade, and operate."
	/>
	<LinkCard
		title="Configuration"
		href={docsBase + 'config/configuration-hub/'}
		description="How settings load, env whitelist, and the full config reference."
	/>
	<LinkCard
		title="Build plugins"
		href={docsBase + 'plugins/extending-with-plugins/'}
		description="Install and author plugins; use the main API guide when you need depth."
	/>
	<LinkCard
		title="Develop the monorepo"
		href={docsBase + 'development/development/'}
		description="pnpm, Turbo, build/test workflow, and optional Cursor agent."
	/>
	<LinkCard
		title="Architecture"
		href={docsBase + 'architecture/architecture/'}
		description="System shape, layering, and ADRs."
	/>
	<LinkCard
		title="Reference (generated)"
		href={docsBase + 'reference/generated/plugin-options/'}
		description="Zod-generated JSON for bundled plugin options and top-level app config."
	/>
</CardGrid>

## Agents & automation

<CardGrid stagger>
	<LinkCard
		title="Agent harness"
		href={docsBase + 'development/agent-harness/'}
		description="Boot, verify, lint, smoke, logs — for humans steering coding agents."
	/>
	<LinkCard
		title="Agent workflow"
		href={docsBase + 'development/agent-workflow/'}
		description="Suggested loop before opening a PR; human review expectations."
	/>
	<LinkCard
		title="Golden rules"
		href={docsBase + 'development/agent-golden-rules/'}
		description="Do / don't for the monorepo; common failures and fixes."
	/>
	<LinkCard
		title="Execution plans"
		href={docsBase + 'exec-plans/readme/'}
		description="Versioned multi-step intent and tech-debt notes in git."
	/>
	<LinkCard
		title="Tool references"
		href={docsBase + 'references/readme/'}
		description="Short pnpm, Turbo, and ESM pages — low-token entry for agents."
	/>
</CardGrid>

## New clone (contributors)

<Steps>
	1. Install **Node 24+** and **pnpm 10+** (see root `package.json` `packageManager` via Corepack).
	2. `pnpm install` and `pnpm run build` at the repository root.
	3. Copy `config/local.example.json` to `config/local.json` and adjust; read the <a href={docsBase + 'config/configuration-hub/'}>Configuration hub</a>.
	4. `pnpm run dev` and open the URL printed in the terminal.
</Steps>

<p>
	<a href={docsBase + 'overview/'}>Full documentation index in git</a>
	{' · '}
	<a href={docsBase + 'e2e/readme/'}>Jira E2E walkthroughs</a>
	{' · '}
	<a href={docsBase + 'reference/changelog/'}>CHANGELOG</a>
</p>
